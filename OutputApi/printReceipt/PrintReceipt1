package iit_cs586.OutputApi.printReceipt;

import iit_cs586.GasStation;

public class PrintReceipt1 implements PrintReceipt {

    @Override
    public void showPrintReceipt() {
        System.out.println("Printing the receipt, total price is " + GasStation.ds1.total + "$ !!!");
        System.out.println();
    }

}
